---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mailu
spec:
  dnsNames:
    - "mail.${SECRET_DOMAIN}"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  privateKey:
    algorithm: ECDSA
  secretName: mailu-tls
  usages:
    - digital signature
