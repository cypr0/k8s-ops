---
apiVersion: v1
kind: Service
metadata:
  name: paperless-mcp
  labels:
    app.kubernetes.io/name: paperless-mcp
spec:
  selector:
    app.kubernetes.io/name: paperless-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
