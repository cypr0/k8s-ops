---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-mcp
  labels:
    app.kubernetes.io/name: nextcloud-mcp
spec:
  selector:
    app.kubernetes.io/name: nextcloud-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
