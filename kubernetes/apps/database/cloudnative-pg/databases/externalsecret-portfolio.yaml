---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: portfolio-db-role
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: portfolio-db-role
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # CNPG managed roles require both username and password keys
        username: portfoliousr
        password: "{{ .PORTFOLIO_POSTGRESQL_PASSWORD }}"
  dataFrom:
    - extract:
        key: portfolio
