---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: roundcube-db-role
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: roundcube-db-role
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        username: roundcubeusr
        password: "{{ .ROUNDCUBE_POSTGRESQL_PASSWORD }}"
  dataFrom:
    - extract:
        key: mailu
