---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: trading-db-role
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: trading-db-role
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        # CNPG managed roles require both username and password keys
        username: tradingusr
        password: "{{ .TRADING_POSTGRESQL_PASSWORD }}"
  dataFrom:
    - extract:
        key: hermes-agent-trading
