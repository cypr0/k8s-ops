---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: logging

components:
  - ../../components/sops

resources:
  - ./namespace.yaml
  - ./opensearch/ks.yaml
  - ./opensearch-dashboards/ks.yaml
