---
# Velero server + node-agent daemonset: both carry app.kubernetes.io/name=velero.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: velero
  namespace: velero
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: velero
  ingress:
    # Backup/restore metrics on 8085. Since the restore-test was removed these
    # are the only evidence that backups are running at all.
    - fromEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": monitoring
            app.kubernetes.io/name: prometheus
      toPorts:
        - ports:
            - port: "8085"
              protocol: TCP
  egress:
    - toEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    - toEntities:
        - kube-apiserver
    # S3-compatible backup storage (Intercolo)
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
