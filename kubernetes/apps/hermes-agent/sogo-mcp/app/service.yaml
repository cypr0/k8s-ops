---
apiVersion: v1
kind: Service
metadata:
  name: sogo-mcp
  labels:
    app.kubernetes.io/name: sogo-mcp
spec:
  selector:
    app.kubernetes.io/name: sogo-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
