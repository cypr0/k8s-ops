---
apiVersion: v1
kind: Service
metadata:
  name: mailu-mcp
  labels:
    app.kubernetes.io/name: mailu-mcp
spec:
  selector:
    app.kubernetes.io/name: mailu-mcp
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
