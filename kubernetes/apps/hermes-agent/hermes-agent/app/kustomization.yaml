---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hermes-agent
resources:
  - ./gitrepository.yaml
  - ./helmrelease.yaml
  - ./rbac.yaml
  - ./ciliumnetworkpolicy.yaml
# No standalone externalsecret.yaml: unlike most charts in this repo, the
# hermes-agent chart renders its own ExternalSecret from HelmRelease values
# (externalSecret.* below) -- see templates/external-secret.yaml upstream.
