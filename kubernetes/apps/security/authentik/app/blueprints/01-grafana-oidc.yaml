version: 1
metadata:
  name: Grafana OIDC
  labels:
    app: grafana

entries:
  # ── Custom scope mapping: groups claim for role derivation ────────────
  - model: authentik_providers_oauth2.scopemapping
    id: grafana-scope-mapping
    identifiers:
      name: grafana-scope
    attrs:
      scope_name: grafana
      description: Grafana roles and group membership
      expression: |
        groups = [group.name for group in request.user.ak_groups.all()]
        return {
            "groups": groups,
        }

  # ── OAuth2 / OIDC Provider ────────────────────────────────────────────
  - model: authentik_providers_oauth2.oauth2provider
    id: grafana-oidc-provider
    identifiers:
      name: grafana-oidc
    attrs:
      authentication_flow: !Find [authentik_flows.flow, [slug, default-authentication-flow]]
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_type: confidential
      client_id: !Env GRAFANA_CLIENT_ID
      client_secret: !Env GRAFANA_CLIENT_SECRET
      redirect_uris:
        - matching_mode: strict
          url: https://grafana.${SECRET_DOMAIN}/login/generic_oauth
      access_code_validity: minutes=1
      access_token_validity: minutes=5
      refresh_token_validity: days=30
      sub_mode: hashed_user_id
      include_claims_in_id_token: true
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, offline_access]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, grafana]]

  # ── Application ───────────────────────────────────────────────────────
  - model: authentik_core.application
    id: grafana-app
    identifiers:
      slug: grafana
    attrs:
      name: Grafana
      provider: !KeyOf grafana-oidc-provider
      meta_launch_url: https://grafana.${SECRET_DOMAIN}
      meta_icon: https://grafana.com/static/assets/img/fav32.png
      policy_engine_mode: all
