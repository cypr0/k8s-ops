machine:
  network:
    disableSearchDomain: true
    nameservers:
      - 1.1.1.1
      - 1.0.0.1
    # CIS Talos 5.4.1: encrypt all inter-node traffic (WireGuard mesh).
    # Cluster discovery (needed to find peers) is already enabled by default.
    kubespan:
      enabled: true
